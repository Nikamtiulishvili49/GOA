1 # შექმენით ცვლადი, სახელად points, რომელიც თავიდან უდრის 50, შემდეგ
# დაა-update-ეთ ამ ცვლადის მნიშვნელობა, რამოდენიმეჯერ. საბოლოოდ კი დაპრინტეთ ეს ცვლადი
1 # პირველი დავალება
points = 50
points += 50
points -= 30
points *= 100
print(points)
2 # გაასწორეთ შემდეგი კოდი:
     # 1age == 156
    # print(("1age)
2 # მეორე დავალება 

age = 156
print((age))


3 #  დაწერეთ პროგრამა, რომელიც შეიცავს ორ integer ტიპის ცვლადს (მაგალითად, a და b),
# შემდეგ გამოიანგარიშეთ ამ ცვლადების ჯამი, სხვაობა, ნამრავლი და განაყოფი. 
3 # მესამე დავალება

a = 19
b = 16

a + b
a - b
a * b
a / b  

print(a + b)
print(a - b)
print(a * b)
print(a / b)



3 # დაწერეთ კოდი შეცდომებით და ქვემოთ მიუწერეთ მისი სწორი ვერსია.
3 # მესამე დავალება


# არასწორი ვერსია
# a = 100
# b = 900
# c = a 
# z = "b"  
# print(c + z) 




# სწორი ვერსია
# a = 100
# b = 900
# c = a 
# z = b

# print(c + z)

